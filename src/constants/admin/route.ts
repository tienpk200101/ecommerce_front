const PATH_ADMIN = {
  HOME: "/admin/home",
  LIST_USER: "/admin/users",
  CREATE_USER: "/admin/users/create",
};

const PATH_ADMIN_USER = {
    DEFAULT: "users",
    CREATE: "create",
    UPDATE: "update"
}

export { PATH_ADMIN, PATH_ADMIN_USER };
